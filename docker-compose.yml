version: '3.8'
services:
  api-gateway:
    build: ./api-gateway
    ports:
      - "8080:8080"
    environment:
      - API_ORDERS_URL=http://api-orders:8081
    depends_on:
      - api-orders
  api-orders:
    build: ./api-orders
    ports:
      - "8081:8081"
      